# yadc-derive

Internal proc-macro crate for [`yadc`](../yadc/). Provides the derive macros (`Dimension`, `Kind`, `Unit`, `Quantity`, `DynQuantity`, `DimensionSerialize`, `DimensionDeserialize`).

**Don't depend on this directly** — use `yadc` and let it re-export what you need.
